apiVersion: v1
kind: Service
metadata:
  name: cicd-service
  namespace: jenkins
  labels:
    app: jenkins-app
spec:
  selector:
    app: jenkins-app
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 80
